# 비밀(키) — 절대 커밋 금지
.env
**/.env
!**/.env.example

# 런타임 프로젝트 상태·생성물 (레포에 넣지 않음)
web/projects/*
!web/projects/.gitkeep
scripts/out/
web/static/gen/

# 파이썬 캐시
__pycache__/
*.pyc

# 설치/다운로드 런타임 로그 (머신마다 다름)
web/.install/
