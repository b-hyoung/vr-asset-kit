# RUN — "시작해줘" 하면 이대로 진행한다 (검증된 실행 런북)

> 사용자가 **"시작해줘"** 또는 **"RUN.md 읽고 진행해줘"** 라고 하면, AI(Claude)는 이 문서를 위에서 아래로 따른다.
> 이건 후삼국 마을 VR을 **실제로 완주하며 검증한 순서**다 (2026-08). 상상이 아니라 실측이다.
> 참조: `harness/flow.md`(★게이트·재시도 루프 구조) · `harness/meta.md`(설계게이트) · `harness/questions.md`(이미지 슬롯) · `harness/scene-loop.md`(배치) · `harness/pipeline.md`(★도구·명령·값) · `templates/ambiguity-probe.md`(강도 스펙트럼) · `lessons.md`(함정).

---

## 단계 0 — 셋업 확인 + 도구 확정
**check-env.ps1** 돌려 빠진 것 확인(모델 다운로드·키·MCP·GPU). 안 된 항목부터 `SETUP.md V1~V4`.

**쓸 도구(검증 기본값 — 바꾸려면 여기서 확정):**
| 역할 | 기본 | 대안(첫 에셋에서 비교 후 교체 가능) |
|---|---|---|
| 컨셉 이미지 | **OpenAI gpt-image-1** | 연결된 이미지 생성 MCP (키 불필요) |
| image→3D | **독립 Hunyuan3D-2**(로컬) | Hyper3D/Rodin, fal (유료), Blender-MCP 통합 |
| 언리얼 조종 | **UnrealClaude MCP**(:3000 REST) | 다른 Unreal 브릿지 MCP |
| 배치 심판 | **OpenAI gpt-4o** | 다른 비전 모델 |
- 이미지·3D는 **swappable** — "깨끗한 컨셉 이미지"만 나오면 어떤 생성기든 OK (`flow.md §3`). 절차·임포트·심판은 고정.
- OpenAI 키는 파일에서만 읽음(값 노출 금지). 언리얼은 임포트 단계 전까진 없어도 됨.

## 단계 1 — 인테이크 질문 (사용자 입으로만)
`meta.md` 8질문. **주제·에셋을 이전 문서/맥락에서 끌어오지 마라 — 반드시 사용자에게 묻는다.**
> ⚠️ 실측 실패(08-24): AI가 **주제(후삼국)를 안 묻고 스스로 잡고** 진행함. 새 세션에서 배경·주제가 이전과 같을 거라 **절대 가정하지 마라.** "무엇을, 어디 배경으로 만들지"부터 사용자에게 확인. 확정 전엔 에셋·스타일 미리 정하지 않는다.
- 결과물 단위(에셋/장면/레벨) → 복합이면 에셋 리스트업 먼저
- 주제 / 대상 기기 / 엔진 / 필수·배제

## 단계 2 — ★맨 처음: 강도 스펙트럼 이미지로 "형(look)"을 고르게 한다  ← 우리가 실제로 맨 처음 한 것
분위기·강도를 **텍스트로 묻지 않는다.** gpt-image로 4장 뽑아 **눈으로 고르게 한다.**
1. 모호어(강도/분위기)를 하나 잡는다 — 예: "판타지풍" + 시간대(노을)
2. `templates/ambiguity-probe.md`의 **공통 베이스 프롬프트**(배제 요소 포함) + **강도 4단계(은은/중간/뚜렷/하이)** 만 갈아끼워 `scripts/gen_prop_image.py` 계열로 4장 생성 — **성벽에서 내려다본 장면 구도**로
3. 사용자에게 4장 보여주고 **하나 고르게 한다**
   - (후삼국 실제: 사용자가 **"뚜렷·노을"** = `spectrum_3_ddwo.png` 선택 — 빛나는 문양·떠다니는 등불·발광 안개)
4. 고른 이미지 = **스타일 앵커 + 강도·시간대 확정.** 이후 모든 에셋·장면 생성에 이 톤을 참조로 주입 (앵커가 바뀌면 사용자 확인)
> 불합격이면 사유를 **배제(슬롯7)** 에 넣고 재생성(최대 3회). 배제는 `ONLY ... NOT ...: no A, no B` 형식으로 강하게.

## 단계 3 — 에셋 리스트 확정·분할 + 브리프 (★부족한 걸 사용자에게 묻고 확정)
앵커를 기준으로 장면에 필요한 에셋을 카테고리로 **AI가 초안 리스트업** → 그다음 **반드시 사용자에게 확인**:
1. AI가 앵커 이미지를 대조해 **필요 에셋 초안** + **빠지기 쉬운 것**을 짚어 제시 (건물/자연물/소품/인물·동물/이펙트/구조물)
2. 사용자에게 **"이 리스트에서 부족하거나 빠진 거 있나요? 더 넣을 거?"** 물어 **채운 뒤 확정.** ⛔ 확정 전엔 제작 시작 안 함
3. 앵커에 보이는데 리스트에 없는 요소(예: 떠다니는 등불·나무·사람·동물·다채 굴뚝연기)를 **AI가 먼저 찾아 제안** — 사용자가 매번 지적하게 두지 않는다
> 실측 실패(08-24): 나무·사람/동물·"여러 채" 굴뚝연기가 리스트에서 빠졌는데 "완성"이라 착각. → 이 확정 게이트로 방지.

(후삼국 실제: 한옥(기와/변형 3종)·우물·나무·바위·당산나무·물레방아 + 인물/가축 + 이펙트[굴뚝연기·모닥불·등불·부적] + 성벽)
→ `templates/scene-brief.md` 채워 `docs/scenes/[이름].md`로 저장. **생성 건수 ≠ 배치 수량**(1~3건 생성 후 복제/변형).

## 단계 4 — 에셋별 제작 루프 (하나씩 만들고 검수하고 넘어간다) — 언리얼 아직 안 켜도 됨
에셋마다 순서대로 (`harness/pipeline.md` §1~3, §3.5):
1. **컨셉 이미지** — `gen_prop_image.py`로 gpt-image-1 (단일객체·투명배경·균일광, 앵커 톤, 배제 명시)
2. **image→3D** — 독립 Hunyuan3D-2 (`gen_props_batch.py`, shape+paint, 감폴리 2만) → glb
3. **★Blender 수정/후처리 (필요시)** — Hunyuan 결과가 완벽하지 않으면 Blender에서 정리 후 다시 export:
   - 스케일을 **실측 크기(미터)** 로, 방향·바닥정렬 · 구멍/플로터/디제너릿 정리 · 필요 시 데시메이트
   - UV 정리(Smart UV) · **발광 요소(창문·불꽃)는 emission 맵을 따로 베이크**(albedo + emit 분리 — 창문 발광은 albedo에 못 굽는다)
   - 정리된 glb로 export. (하드서피스가 image→3D로 잘 나오면 이 단계 생략 가능)
4. **검수 게이트** — (렌더로) 사용자 판정. **AI 자기 채점 금지.**
> 형태 통제가 필요하면(집 변형 등) **블록아웃-레퍼런스**: Blender 회색박스 → gpt-image로 스타일 입힘 → 형/구도 고정.

## 단계 4.5 — 언리얼 임포트 (여기서부터 언리얼 켠다)
`import_ai_all.py`로 정리된 glb들을 언리얼에. ★**Nanite 끄기**, ★임포트 텍스처로 `M_*_AI` 머티리얼 직접 생성해 assign. (에셋을 다 만든 뒤 한 번에 켜서 임포트해도 됨 — 그전 이미지·3D·Blender는 언리얼 없이 가능.)

## 단계 5 — 장면 조립 + 자동 배치 루프 (자율)
`scene_place_loop.py` → `assemble.py`(조립+SceneCapture 렌더) → `cmp_openai_scene.py`(gpt-4o JSON: score+actions) → 재배치 반복.
- 액션: move/move_house/spread_houses/jitter_house_rot/scatter_houses (정렬 지적 정체 시 scatter, 1회가 최선)
- 성공 판정 = **village.png 파일 존재**로 (UE 로그 길어 출력 파싱 불가)

## 단계 6 — 조명·무드 (사람이 고르고, 사람이 판정) — `pipeline.md §7` 프리셋 표
- ⛔ **노을 고정 아님.** 시간대는 사용자가 고른다(웹 '조명·시간대 선택' → `inputs.lighting`).
  안 골랐으면 **조명 미개입**. 적용: `py -u scripts/apply_lighting.py --project <pid>`
- 예) 노을: 태양 **pitch -2~-3 + temperature 3000~3800K**, 라이트 색은 **`set_light_color(LinearColor)`**
- ⛔ **기본 SkyLight 절대 미개입** (프리셋과 무관하게 항상)
- 발광 머티리얼(창문·불꽃) + PointLight(등불·모닥불, `set_light_color` 따뜻)

## 단계 7 — 성벽/구도 (선택)
성벽 블록아웃(본체+흉벽+계단) → 카메라를 성벽 너머로 → "성벽에서 내려다본 마을" 구도. 멀리서 관망 원하면 카메라 후퇴+FOV 축소.

## 단계 8 — 내보내기 (애플비전/외부)
`export_glb.py` → 자체완결 GLB(메시+텍스처+조명 KHR_lights_punctual 포함). Mac에서 Reality Converter로 USDZ. **하늘·안개·노출은 안 감** → 타깃 재세팅.

## 단계 9 — 회고
이번 회전에서 실제로 막힌 것만 `lessons.md`에 출처·날짜로 올린다. 상상으로 규칙 추가 금지.

---

## ⛔ 하드룰
1. **기본 SkyLight·기본 템플릿/레벨/액터 건드리지 마라.** 새 사본에서 작업.
2. **사용자가 손 수정한 뒤 assemble.py 재실행 금지** (clear+재빌드라 수정이 날아감). 개별 액터 스크립트로.
3. **라이트 색 = `set_light_color(LinearColor)`** (`unreal.Color()` 위치인자는 BGRA로 파랑 뒤집힘).
4. **저폴리 Nanite 끄기. 렌더는 SceneCapture2D(RTF_RGBA8).**
5. **API 키는 파일에서만, 값 노출 금지.**
6. **AI 자기 채점 금지.** 미적 판정은 사용자 or OpenAI 독립 심판.
7. **모호어는 스펙트럼 4장으로.** 텍스트로 안 묻는다.
8. **주제·배경을 스스로 잡지 마라.** 새 세션에서 이전과 같을 거라 가정 금지 — 사용자에게 먼저 묻는다.
