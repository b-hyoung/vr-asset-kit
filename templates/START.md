# START — 이 파일을 AI에게 주면 바로 이어서 진행됩니다

> **템플릿 사용법**: 대괄호 `[ ]` 자리를 전부 채우고 이 인용 블록은 지운다.
> 빈 대괄호가 남아 있으면 진행 금지 — 사용자에게 묻는다.

당신(AI)에게: 이 문서는 진행 중인 프로젝트의 인수인계서다. 아래 내용만으로 즉시 "다음 작업"을 실행하라.
사용자에게 이미 확정받은 결정을 다시 묻지 마라.
공용 하네스를 먼저 읽어라: `HARNESS.md`에 적힌 경로의 `harness/meta.md`, `scene-loop.md`, `questions.md`.

## 프로젝트
[한 줄 정의 — 무엇을, 어느 기기용으로, 어떤 체험으로]
방식: 하네스(MD 규칙) 기반 루프 — AI가 만들고, 사용자는 보고 고르기만 한다.

## 확정된 결정 (재질문 금지)
- 결과물 단위: [에셋 / 장면 / 레벨] — [구체 이름]
- 대상 기기·플랫폼: [예: Meta Quest 3 / PCVR]
- 엔진·프로젝트: [예: 언리얼 5.8 VR 템플릿 — 생성 여부]
- 에셋 [N]종: [이름(제작 경로), ...]
- 이미지 생성: [모델] (키는 환경변수 [VAR], 파일·채팅에 쓰지 말 것)
- 3D 변환: [도구 / 비교 대상]
- 미적 판단은 텍스트로 묻지 않는다 — 이미지를 생성해 보여주고 고르게 한다

## 환경 준비 상태
공용 하네스 `SETUP.md` 기준. 아직 안 된 항목이 있으면 그것부터 처리한다.

- [ ] A. 소프트웨어 (Blender / 언리얼 [버전] / uv / node / git)
- [ ] B. MCP 3종 등록 (Blender / Unreal / 이미지 생성)
- [ ] C. API 키 — 이미지 생성: [경로], 3D 변환 키는 Blender 패널에 입력
- [ ] D. 연결 확인 — Blender 실행 + MCP 서버 Start, 언리얼 프로젝트 열림

★ **임포트 단계 전까지는 언리얼이 없어도 진행 가능하다.** 이미지 생성·3D 변환은 먼저 돌려도 된다.

## ▶ 다음 작업 (지금 바로 실행)
[구체적 지시 1~2줄. "무엇을 만들어 사용자에게 보여주고 무엇을 고르게 하라"]

[필요 시 사용할 프롬프트를 그대로 여기 적는다 — templates/ambiguity-probe.md 참고]

## 선택 후 할 일
1. 사용자가 고른 것을 `docs/scenes/[장면].md`에 확정 정의로 기록
2. 선택된 이미지 = "스타일 앵커" — 이후 모든 에셋 이미지 생성에 참조로 포함
3. 다음: 에셋별 이미지 생성(질문 시트 슬롯 8개를 채워서) → 3D 변환 → [첫 에셋]부터
4. 그 뒤: 엔진 프로젝트 생성 → 임포트 → 자연 배치 → [N]각도 캡처 검증
5. 회고: 이번 회전에서 드러난 규칙을 하네스에 추가

## 금지 사항 (기록된 실수)
- 장면 요청을 단일 에셋 작업으로 축소하지 말 것
- 모호어를 해석하지 말고 보기로 만들어 고르게 할 것
- AI 자기 채점 금지 — 합·불 판정은 사용자가 한다
- 질문 시트 슬롯이 비어 있으면 이미지 생성 금지
