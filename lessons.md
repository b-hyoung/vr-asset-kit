# 회고 로그 — 규칙의 출처

하네스 규칙은 **사전 상상이 아니라 실제 실패에서** 나온다.
규칙을 추가할 때마다 여기에 출처(프로젝트·날짜·무슨 일이 있었는지)를 남긴다.
출처가 없는 규칙은 삭제 후보다.

| 날짜 | 프로젝트 | 무슨 일이 있었나 | 승격된 규칙 |
|---|---|---|---|
| 2026-08-21 | 후삼국 | 사용자가 "성 밖에서 보이는 마을" **장면**을 요청했는데 루프를 "단일 에셋 파일럿"으로 축소해 설계함 | `meta.md` §1 — 결과물 단위를 최우선으로 확정, 복합이면 에셋 리스트업부터 |
| 2026-08-21 | 후삼국 | 브리프의 "판타지풍"을 AI가 임의 해석 | `meta.md` §8 — 모호어는 스펙트럼 4장으로 분해해 사용자가 고르게 함. 절차는 `templates/ambiguity-probe.md` |
| 2026-08-21 | 후삼국 | AI가 산출물을 스스로 합격 처리하려 함 | 관통 원칙 — 자기 채점 금지, 판정은 사용자 |
| 2026-08-22 | 후삼국 | 프롬프트 빈칸을 모델이 임의로 채워 원하는 이미지가 안 나옴 | `questions.md` — 슬롯 8개 미충족 시 생성 금지, 배제 슬롯(7)이 최우선 |
| 2026-08-24 | — | 하네스가 후삼국 프로젝트 안에 묶여 있어 새 프로젝트에 재사용 불가 | 하네스를 `vr-harness/`로 분리, 프로젝트는 `HARNESS.md`로 참조 |

## 아직 검증되지 않은 것 (규칙 아님)

후삼국 프로젝트는 **하네스 설계까지만 진행되고 실제 에셋 제작에 들어가지 않았다.**
따라서 아래는 실전 검증을 거치지 않은 가정이다 — 첫 회전 후 회고에서 확정하거나 폐기한다.

- 자연 배치 수치(지터 ±10%, 경사 20°)
- 재시도 상한 3회
- 이미지→3D 도구 선택 (Hyper3D vs Hunyuan3D 비교 미실시)
- 절차적 모델링 경로가 실제로 쓸 만한 결과를 내는지

## 2026-08-24 추가 — 3D 생성 경로를 몰라서 준비 목록이 얕았던 건

설치된 blender-mcp 소스를 실제로 읽고 확인한 사실:

- 3D 생성은 **비동기 4단**(생성 요청 → 폴링 → GLB 임포트 → **world_bounding_box 크기 보정**).
  4단을 빼면 임의 스케일 메시가 들어와 실측 판정을 통과할 수 없다.
- **키 모드가 플로우를 바꾼다**: Hyper3D `MAIN_SITE`=로컬 절대경로 / `FAL_AI`=이미지 URL,
  Hunyuan3D `OFFICIAL_API`=3단 / `LOCAL_API`=1단. 준비 단계에서 모드를 정해야 한다.
- Hyper3D·Hunyuan3D는 **단일 오브젝트 생성기**. 장면 전체·지면 생성·부품 조립 금지.
- 같은 에셋 다수 배치는 **재생성이 아니라 파이썬 복제** + 지터.
- 기성 에셋(PolyHaven/Sketchfab)이 별도 경로로 존재. HDRI 환경광은 생성 대상이 아니다.

→ `SETUP.md` B4·B5 신설, `harness/scene-loop.md`에 4단 절차·한계·복제 규칙 추가.

## 2026-08-24 추가 — 머티리얼·텍스처 단계가 하네스에 아예 없었던 건

사용자 지적: "머터리얼하고 이런건 어디서 함? 문서에 어디서 하는지 모르겠는데"
→ 맞다. 하네스는 **형태만** 다루고 표면은 통째로 빠져 있었다.

addon 소스에서 확인한 사실:

- 표면 작업량은 **에셋 경로에 따라 완전히 다르다**:
  다운로드·생성 에셋은 머티리얼 내장, **절차적 에셋만 표면이 없다**.
- 절차적 메시 3단: UV 언랩 → `download_polyhaven_asset(asset_type="textures")` → `set_texture`
- ★ **UV 함정**: `set_texture`는 Principled 그래프를 **UV 좌표에 연결**하지만
  UV를 만들지도 확인하지도 않는다. 절차적 메시는 UV가 없어 텍스처가 깨진다.
  텍스처가 이상하면 머티리얼이 아니라 UV를 본다.
- **순서**: HDRI 환경광을 먼저 넣고 머티리얼을 판정한다. 조명 없이 판정하면 전부 다시 해야 한다.
- 소스 우선순위가 서버 프롬프트에 명시돼 있다 — **절차적 모델링은 최후 수단**.
  (실존물=Sketchfab → PolyHaven / 일반물=PolyHaven → Sketchfab / 고유물=생성 / 환경광=HDRI / 텍스처=PolyHaven)
  후삼국 브리프는 초가집·목책을 처음부터 "절차적"으로 못박아 이 판단을 건너뛰었다.

→ `harness/scene-loop.md`에 머티리얼 절차·UV 함정·소스 우선순위 추가,
  `meta.md` §3을 "제작 경로 **및 표면**"으로 확장, `templates/scene-brief.md`에 표면 열·표면 방침 추가.

### 여전히 미검증 (사실처럼 쓰지 말 것)
- Blender → 언리얼 머티리얼 이관 충실도
- Quest 모바일 GPU 머티리얼·텍스처 예산

---

## 2026-08-24 추가 — 언리얼 직접 구동으로 실제 완주하며 나온 규칙 (검증본)

| 날짜 | 프로젝트 | 실패/발견 | 승격된 규칙 |
|---|---|---|---|
| 08-24 | 후삼국 | image→3D를 Blender MCP 통합으로만 계획했는데, 실제론 **독립 Hunyuan3D-2**(standalone)가 더 통제됨 | `SETUP.md V2` — 독립 Hunyuan 셋업(CUDA·빌드·모델다운·함정)을 실측으로 |
| 08-24 | 후삼국 | 언리얼을 Blender 경유가 아니라 **UnrealClaude MCP로 직접** 조종 (임포트·스폰·배치·렌더) | `SETUP.md V3` + `pipeline.md §4` — STDIO 노드 브릿지 등록, execute_script REST(:3000), 자동승인 |
| 08-24 | 후삼국 | **저폴리 임포트 메시가 Nanite면 렌더에 안 보임** | Nanite 끄기 규칙 (`pipeline.md §3`) |
| 08-24 | 후삼국 | 에디터 백그라운드면 뷰포트 캡처/HighResShot이 **stale** | **SceneCapture2D로 렌더**, RT는 RTF_RGBA8 (아니면 export가 HDR로 깨짐) |
| 08-24 | 후삼국 | glTF 임포트가 텍스처는 넣는데 **base color 미연결** | 임포트 텍스처로 `M_*_AI` 머티리얼 직접 생성 |
| 08-24 | 후삼국 | ★**라이트가 다 파랗게** 나옴 | `unreal.Color(r,g,b)` 위치인자가 **FColor BGRA로 R↔B 뒤집힘** → **`set_light_color(LinearColor(r,g,b,1))`** 로. 태양 색도 같은 버그라 고치니 SkyAtmosphere 노을까지 정상화 |
| 08-24 | 후삼국 | 노을이 안 나옴(주간 블루) | 태양 **pitch 0 근처(-2~-3)** + **use_temperature+temperature 3000~3800K**. ⛔**기본 SkyLight 미개입**(사용자 하드룰) |
| 08-24 | 후삼국 | 배치 심판이 "집 정렬"에 점수 정체 | `scatter_houses`(2D 산포) 액션. 다회는 과산포 → **1회가 최선** |
| 08-24 | 후삼국 | 사용자 수동 수정 후 재조립하면 다 날아감 | **수동 수정 뒤 assemble 재실행 금지.** 개별 액터만 손대는 스크립트로. 아웃라이너 폴더링은 `set_folder_path`라 안전 |
| 08-24 | 후삼국 | 애플비전 이관: USD 레벨 export가 파일을 안 남김 | **자체완결 GLB로 export**(메시+텍스처+조명 KHR_lights_punctual). Mac에서 USDZ 변환. 하늘·안개·노출은 안 감 |

| 08-24 | 후삼국 | ★AI가 **주제(후삼국 배경)를 사용자에게 안 묻고** 이전 세션/맥락에서 스스로 잡고 진행함 (사용자가 원한 방향이 아닐 수 있음) | 새 세션·새 작업 시작 시 **주제·배경은 반드시 사용자 입으로 확정.** 이전 프로젝트·문서·맥락에서 끌어오기 금지. `RUN.md §1` + `meta.md §1` 하드룰. 확정 전엔 에셋·스타일 어느 것도 미리 정하지 않는다 |

| 08-24 | 후삼국 | 에셋 리스트에서 **나무·사람/동물·"여러 채" 굴뚝연기가 빠졌는데** "완성"이라 착각(사용자가 매번 지적) | `RUN.md §3` — AI가 앵커 대조로 **빠진 에셋을 먼저 찾아 제안** + **"부족·빠진 거 있나요?" 사용자에게 물어 확정** 후 제작. 확정 게이트 통과 전 제작 시작 금지 |
