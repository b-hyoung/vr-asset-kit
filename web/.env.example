# 이 파일을 web/.env 로 복사하고 값을 채우세요. (web/.env 는 .gitignore 로 커밋되지 않음)
# 키 탐색 우선순위: 환경변수 OPENAI_API_KEY → 환경변수 VRKIT_ENV_FILE(경로) → web/.env → ~/.vrkit/.env
# ★ 특정 프로젝트 폴더(.env) 경로를 코드에 하드코딩하지 않는다. 여기(web/.env) 또는 VRKIT_ENV_FILE 로만.

OPENAI_API_KEY=
# 선택: 클라우드 3D(Rodin/Hyper3D) 사용 시
RODIN_API_KEY=
